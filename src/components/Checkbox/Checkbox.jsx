import React from 'react';
import './Checkbox.css'

const Checkbox = (props) => {
  const {
    className = '',
    isChecked,

  }




  return (
    <label className={`${className}`}>

    </label>
  );
};

export default Checkbox;